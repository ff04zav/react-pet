import React from "react";
import Form from "../Form";
import Table from "../Table";

const Management = () => {
  return (
    <div className="App">
      <Form />
      <Table />
    </div>
  );
};

export default Management;
